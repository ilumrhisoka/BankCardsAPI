💳 CyberBank: Система Управления Банковскими Картами и Счетами
Описание Проекта

CyberBank — это полнофункциональное RESTful API для управления банковскими картами, счетами, денежными переводами и пользовательскими аккаунтами, дополненное простым, но стильным веб-интерфейсом. Система разработана с использованием Spring Boot и предоставляет функциональность как для обычных пользователей (просмотр своих карт и счетов, совершение переводов, управление контактами, поддержка), так и для администраторов (полное управление картами, пользователями, счетами и запросами поддержки). Особое внимание уделено безопасности, стандартизированной обработке ошибок и удобной документации API.
✨ Ключевые Возможности
👤 Управление Пользователями

    Регистрация и Аутентификация: Безопасная регистрация и вход в систему с использованием JWT (JSON Web Tokens).
    Управление Профилем: Просмотр и обновление профиля, установка ежемесячного лимита трат.
    Администрирование Пользователей: Администраторы имеют полный CRUD (создание, просмотр, обновление, удаление) для всех пользователей.

💰 Управление Счетами и Картами

    Создание Счетов: Пользователи могут создавать различные типы банковских счетов (текущие, сберегательные).
    Создание Карт: Администраторы могут выпускать новые банковские карты для пользователей, привязывая их к существующим счетам.
    Детализация Карт: Пользователи видят только свои карты, администраторы — все карты в системе.
    Запросы на Блокировку/Разблокировку: Пользователи могут отправлять запросы на блокировку или разблокировку своих карт.
    Административное Управление Картами: Администраторы могут напрямую блокировать/активировать карты, а также одобрять/отклонять запросы пользователей на изменение статуса карты.
    Пополнение/Снятие: Администраторы могут пополнять баланс карт и снимать с них средства.
    Общий Баланс: Пользователи могут просматривать общий баланс по всем своим картам и счетам.
    Шифрование Карт: Номера карт хранятся в зашифрованном виде для повышения безопасности.

💸 Денежные Переводы

    Переводы Между Картами: Пользователи могут инициировать денежные переводы между своими картами или на карты других пользователей.
    История Переводов: Просмотр подробной истории всех своих переводов или переводов по конкретной карте.
    Быстрые Переводы: Возможность сохранять контакты (номера карт) для быстрых переводов.

🆘 Поддержка и Уведомления

    Система Тикетов: Пользователи могут создавать и просматривать свои обращения в службу поддержки.
    Администрирование Тикетов: Администраторы могут просматривать открытые/в работе тикеты, назначать их себе и закрывать.
    Уведомления: Система отправляет пользователям уведомления о важных событиях (переводы, начисление комиссий, изменение статуса тикетов). Пользователи могут просматривать все уведомления и помечать их как прочитанные.

📊 Финансовые Услуги

    Сервисные Комиссии: Возможность начисления и обработки различных типов комиссий (ежемесячное обслуживание, переводы и т.д.).
    Курсы Валют: Интеграция с API Национального банка Республики Беларусь для отображения актуальных курсов валют (USD, EUR, RUB).

🌐 Веб-Интерфейс

    Аутентификация: Страницы входа и регистрации.
    Панель Пользователя: dashboard.html - интерактивная панель с обзором карт, балансов, быстрых переводов, лимитов трат, статистики и последних транзакций.
    Панель Администратора: admin_dashboard.html - панель для управления всеми сущностями системы (карты, пользователи, тикеты).

🚀 Используемые Технологии

    Backend:
        Java 21
        Spring Boot 3.5.5
        Spring Data JPA (Hibernate)
        Spring Security (JWT, BCrypt)
        Maven
        Liquibase (для управления миграциями базы данных)
        MapStruct (для маппинга DTO и сущностей)
        Lombok (для сокращения бойлерплейт-кода)
        dotenv-java (для загрузки переменных окружения из .env)
        jjwt (для работы с JWT)
        springdoc-openapi-starter-webmvc-ui (для Swagger UI)
        AES/GCM (для шифрования номеров карт)
    Frontend (Web UI):
        Thymeleaf (шаблонизатор)
        Bootstrap 5 (CSS-фреймворк)
        Chart.js (для графиков)
        Vanilla-Tilt.js (для эффектов наведения)
        Canvas-Confetti (для анимации успешных операций)
        RoboHash (для генерации аватаров)
        Font Awesome (иконки)
        API Национального банка Республики Беларусь (для курсов валют)
    База Данных: PostgreSQL
    Контейнеризация: Docker, Docker Compose
    Документация API: Springdoc OpenAPI (Swagger UI)

🛠️ Предварительные Требования

Для запуска проекта вам потребуется установленное следующее программное обеспечение:

    Git
    Java Development Kit (JDK) 21
    Maven
    Docker Desktop (включает Docker Engine и Docker Compose)

🏁 Запуск Проекта

Проект использует Docker Compose для запуска базы данных PostgreSQL, а само Spring Boot приложение запускается локально.
1. Клонирование Репозитория

Откройте терминал или командную строку и выполните следующую команду:
bash

git clone https://github.com/ilumrhisoka/Bank_REST
cd Bank_REST # Перейдите в корневую папку проекта

2. Настройка Переменных Окружения (файл .env)

Проект использует переменные окружения для конфиденциальных данных. Создайте файл с именем .env в корневой директории проекта (там же, где находится docker-compose.yml и application.yml).

Пример содержимого файла .env:
properties

# --- Конфигурация Базы Данных для Docker Compose ---
DB_NAME=bank_db
DB_USER=bank_user
DB_PASSWORD=bank_pass

# --- Секретный Ключ JWT ---
# Должен быть длинной, случайной и уникальной строкой для безопасности.
JWT_SECRET=your_super_secret_jwt_key_that_is_very_long_and_random

# --- Учетные Данные Администратора по Умолчанию ---
# Эти данные используются для создания первого администратора при запуске приложения,
# если ни один администратор еще не существует в базе данных.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password

# --- Ключ Шифрования для Номеров Карт ---
# Должен быть 16, 24 или 32 символа для AES-128, AES-192 или AES-256 соответственно.
# Генерируйте уникальный ключ!
ENCRYPTION_KEY=your_encryption_key_16_chars

Важно:

    Замените значения после знака = на свои собственные, уникальные и безопасные.
    Файл .env добавлен в .gitignore, поэтому он не будет отслеживаться Git'ом.

3. Запуск Базы Данных с помощью Docker Compose

В корневой директории проекта, где находится файл docker-compose.yml, выполните следующую команду, чтобы запустить контейнер PostgreSQL:
bash

docker-compose up -d

    docker-compose up: Запускает сервисы, определенные в docker-compose.yml (в данном случае, только postgres).
    -d: Запускает сервисы в фоновом режиме (detached mode).

Подождите несколько секунд, пока контейнер PostgreSQL полностью запустится. Вы можете проверить его статус:
bash

docker-compose ps

4. Запуск Spring Boot Приложения

После запуска базы данных, вы можете запустить Spring Boot приложение локально. Убедитесь, что у вас установлен JDK 21 и Maven.
bash

# Соберите проект
mvn clean install

# Запустите приложение
mvn spring-boot:run

Приложение будет доступно по адресу http://localhost:8081.
5. Проверка Работоспособности

Откройте ваш веб-браузер и перейдите по следующим адресам:

    Веб-интерфейс: http://localhost:8081/ (будет перенаправлен на /login или /register при первом запуске).
    Документация API (Swagger UI): http://localhost:8081/swagger-ui.html

🌐 Доступ к Приложению
Веб-Интерфейс

Приложение предоставляет простой веб-интерфейс для взаимодействия с системой:

    Страница входа: http://localhost:8081/login
    Страница регистрации: http://localhost:8081/register
    Панель пользователя: http://localhost:8081/dashboard (доступна после входа)
    Панель администратора: http://localhost:8081/admin/dashboard (доступна после входа под учетной записью администратора)

Документация API (Swagger UI)

Интерактивная документация API доступна по адресу:
http://localhost:8081/swagger-ui.html
Здесь вы можете ознакомиться со всеми эндпоинтами, их параметрами, примерами запросов и ответов, а также протестировать API.
📖 Примеры Использования

Этот раздел демонстрирует ключевые сценарии использования через веб-интерфейс и API.
1. Первоначальная Настройка и Вход

   Первый запуск: Приложение автоматически создаст пользователя-администратора, если его нет.
   Username: admin (или значение из ADMIN_USERNAME в .env)
   Password: password (или значение из ADMIN_PASSWORD в .env)

   Вход в систему:
   Откройте http://localhost:8081/login в браузере.
   Введите учетные данные администратора (admin/password) и войдите. Вас перенаправит на http://localhost:8081/admin/dashboard.
   Либо зарегистрируйте нового пользователя через http://localhost:8081/register и войдите под ним. Вас перенаправит на http://localhost:8081/dashboard.

2. Сценарии для Администратора (через admin_dashboard.html или Swagger UI)

После входа под учетной записью администратора (admin/password), перейдите на http://localhost:8081/admin/dashboard.

    Создание новой карты для пользователя:
        Нажмите кнопку "NEW CARD".
        В модальном окне выберите пользователя из списка (например, только что зарегистрированного).
        Нажмите на кнопку с иконкой <i class="fas fa-random"></i>, чтобы сгенерировать номер карты.
        Введите имя держателя карты.
        Нажмите "CREATE CARD".
        Вы увидите новую карту в списке.

    Управление балансом карты:
        Найдите нужную карту в списке.
        Используйте кнопки "+" (пополнить) или "-" (снять) для изменения баланса.
        Введите сумму в модальном окне и нажмите "EXECUTE".

    Блокировка/Активация карты:
        Нажмите на иконку <i class="fas fa-lock"></i> (заблокировать) или <i class="fas fa-unlock"></i> (активировать) рядом с картой.

    Обработка запросов пользователей:
        Если пользователь отправил запрос на блокировку/разблокировку, он появится в секции "ATTENTION REQUIRED".
        Нажмите "YES" для одобрения или "NO" для отклонения запроса.

    Просмотр и управление тикетами поддержки:
        Перейдите на вкладку "TICKETS".
        Вы увидите список открытых тикетов. Нажмите "Take Ticket", чтобы назначить тикет себе, или "Close Ticket", чтобы закрыть его.

3. Сценарии для Обычного Пользователя (через dashboard.html или Swagger UI)

После входа под учетной записью обычного пользователя, перейдите на http://localhost:8081/dashboard.

    Просмотр своих карт и баланса:
        На главной панели вы увидите список своих карт с замаскированными номерами и текущими балансами.
        Общий баланс отображается в навигационной панели.

    Просмотр деталей карты:
        Нажмите на любую из своих карт, чтобы открыть модальное окно с подробной информацией (включая CVV, который генерируется динамически).
        Вы можете скопировать номер карты, нажав на него.

    Запрос на блокировку/разблокировку карты:
        В модальном окне карты нажмите "BLOCK" или "UNBLOCK", чтобы отправить запрос администратору. Статус карты изменится на PENDING_BLOCK или PENDING_UNBLOCK.

    Денежный перевод:
        Нажмите кнопку "ПЕРЕВОД" на главной панели.
        Выберите карту-источник.
        Выберите тип перевода: "Клиенту" (по номеру карты) или "Свои счета" (между вашими картами).
        Введите номер карты получателя или выберите свою карту.
        Укажите сумму и нажмите "Отправить".
        При успешном переводе вы увидите анимацию конфетти!

    Управление быстрыми переводами (контактами):
        В секции "Быстрый перевод" нажмите "New", чтобы добавить контакт (имя и номер карты).
        Нажмите на существующий контакт, чтобы быстро заполнить номер карты получателя в форме перевода.
        Наведите на контакт, чтобы увидеть кнопку удаления.

    Установка ежемесячного лимита трат:
        В секции "Лимит (Мес.)" нажмите на иконку <i class="fas fa-pencil-alt"></i>, чтобы изменить свой ежемесячный лимит трат.

    Просмотр истории транзакций:
        В нижней части панели отображается список "Последние транзакции".

    Создание тикета поддержки:
        Нажмите кнопку "Help" в навигационной панели.
        В модальном окне введите тему и описание проблемы, затем нажмите "Создать".
        Ваш тикет появится в списке "ИСТОРИЯ".

    Просмотр уведомлений:
        Нажмите на иконку колокольчика <i class="far fa-bell"></i> в навигационной панели, чтобы увидеть новые уведомления.

⚠️ Обработка Ошибок

API использует стандартизированный подход к обработке ошибок. В случае возникновения ошибки (например, неверный запрос, не найден ресурс, запрещен доступ), вы получите JSON-ответ в формате ErrorResponse, который включает:
json

{
"timestamp": "2025-08-24 12:34:56",
"status": 400,
"error": "Bad Request",
"message": "Insufficient funds on source card. Available: 100.00, Requested: 200.00",
"path": "/api/user/transfers",
"errors": {
"fieldName": "Validation error message"
}
}

Все кастомные исключения (например, CardNotFoundException, InsufficientFundsException, ForbiddenException) обрабатываются глобальным обработчиком, обеспечивая консистентный и предсказуемый формат ошибок.
🔒 Безопасность и Аутентификация

API защищено с использованием Spring Security и JWT (JSON Web Tokens).

    Для доступа к большинству эндпоинтов требуется аутентификация.
    Используются роли ROLE_USER и ROLE_ADMIN для управления доступом к различным ресурсам.
    JWT токены передаются через HTTP-куки (accessToken) для веб-интерфейса или через заголовок Authorization: Bearer <token> для прямых API-запросов (например, из Postman или Swagger UI).
    Пароли пользователей хранятся в зашифрованном виде с использованием BCrypt.
    Конфиденциальные данные, такие как номера карт, шифруются с использованием AES/GCM перед сохранением в базе данных.
